{
  "name": "metallokonstrukcii-site",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for ООО B2E metal structures production.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check scripts/build.mjs && node --check scripts/smoke.mjs && node --check scripts/tz-audit.mjs && node --check src/main.js && npm test && npm run build && npm run audit:tz && npm --prefix worker run check",
    "audit:tz": "node scripts/tz-audit.mjs",
    "smoke": "node scripts/smoke.mjs",
    "start": "node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs && npm --prefix worker test",
    "worker:dev": "npm --prefix worker run dev",
    "worker:deploy": "npm --prefix worker run deploy",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
